<Window x:Class="Puzzle15.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Puzzle15"
        mc:Ignorable="d"
        Title="15 Puzzle MVVM" Height="500" Width="400"
        Closing="Window_Closing">

    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <Style x:Key="TileButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Background" Value="#DDDDDD"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsEmpty}" Value="True">
                    <Setter Property="Visibility" Value="Hidden"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <UniformGrid Rows="1" Columns="2" Margin="0,0,0,10">
            <StackPanel HorizontalAlignment="Center">
                <TextBlock Text="MOVES" FontWeight="Bold" Foreground="Gray" HorizontalAlignment="Center"/>
                <TextBlock Text="{Binding CurrentMoves}" FontSize="20" HorizontalAlignment="Center"/>
            </StackPanel>
            <StackPanel HorizontalAlignment="Center">
                <TextBlock Text="BEST" FontWeight="Bold" Foreground="Gray" HorizontalAlignment="Center"/>
                <TextBlock Text="{Binding BestScoreDisplay}" FontSize="20" HorizontalAlignment="Center"/>
            </StackPanel>
        </UniformGrid>

        <ItemsControl Grid.Row="1" ItemsSource="{Binding Tiles}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Rows="{Binding Rows}" Columns="{Binding Cols}"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Style="{StaticResource TileButtonStyle}"
                            Content="{Binding Display}"
                            Command="{Binding DataContext.MoveCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                            CommandParameter="{Binding}"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="New Game" Command="{Binding NewGameCommand}" Padding="20,10" Margin="5"/>
            <Button Content="Save &amp; Quit" Command="{Binding SaveCommand}" Padding="20,10" Margin="5" Click="CloseBtn_Click"/>
        </StackPanel>
    </Grid>
</Window>